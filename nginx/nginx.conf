# Placeholder nginx config for local reverse proxy.
# Update upstreams/routes as your frontend and APIs evolve.

server {
  listen 80;
  server_name _;

  location / {
    return 200 'nginx placeholder config\n';
    add_header Content-Type text/plain;
  }
}
